---
layout: default
title: 2.59 — Compose Least Significant Byte
permalink: /02/2_59/
---

## Problem
Write a C expression that will yield a word consisting of the least significant byte of `x` and the remaining bytes of `y`. For operands `x = 0x89ABCDEF` and `y = 0x76543210`, this would give `0x765432EF`.

## Solution
{% highlight c %}{% include_relative ../../src/02/2_59.c %}{% endhighlight %}

## Explanation
Mask off the least significant byte from `y` and OR in the least significant byte from `x`.
